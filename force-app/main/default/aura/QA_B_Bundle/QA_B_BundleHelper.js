({
	doSubmitHelper : function(cmp) { 
        
        var QABForm = cmp.get("v.QABForm");
        cmp.set("v.nameshem.qazwsx1__shem3_API__c",checkBoxValue);
        //

        var res = cmp.get("c.SetData");   

       
		console.log(QABForm);
                action.setParams({
            QABForm : QABForm
        });            
}})